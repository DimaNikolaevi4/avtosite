# Чеклист разработки сети новостных сайтов (12 тематик)

**Файл:** `CHECKLIST.md`
**Обновлено:** Май 2026 (инициация проекта)
**Проект:** Автоматизированная сетка из 12 новостных сайтов на базе ИИ (Qwen 2.5 7B) и хостинга Beget.com.

## Условные обозначения

| Значок | Значение |
|---|---|
| ✅ | Сделано и работает |
| ⚠️ | Сделано частично / нужна доработка |
| ❌ | Не сделано |
| ⛔ | Заблокировано (требует внешнего ресурса / решения) |

---

## 0. Структура репозитория

| Задача | Статус | Примечание |
|---|---|---|
| 0.1 Создать в корне папку `docs` для информационных файлов (`PROJECT.md`, `CHECKLIST.md`) | ❌ | |
| 0.2 Создать в корне папку `scripts` для PHP-скриптов | ❌ | |
| 0.3 Создать в корне папку `assets` для статичных файлов (CSS, изображения) | ❌ | |
| 0.4 Инициализировать Git-репозиторий и сделать первый коммит | ❌ | |

---

## 1. Окружение и инфраструктура

### 1.1 Развертывание Qwen 2.5 7B на платформе Qubu.ai

| Задача | Статус | Примечание |
|---|---|---|
| 1.1.1 Авторизоваться на платформе Qubu.ai под предоставленными логином и паролем | ❌ | Промпт: `Перейди на https://qubu.ai и авторизуйся, используя логин: {логин} и пароль: {пароль}.` |
| 1.1.2 Перейти в раздел создания проекта или модели | ❌ | Промпт: `На главной странице личного кабинета найди кнопку «Создать проект» или «Новая модель». Перейди в этот раздел.` |
| 1.1.3 Выбрать модель `qwen2.5:7b-instruct-q5_K_M` для развертывания | ❌ | Промпт: `В строке поиска введи запрос: qwen2.5 7b instruct. Из выпадающего списка выбери модель qwen2.5:7b-instruct-q5_K_M. Нажми кнопку «Развернуть» или «Добавить».` |
| 1.1.4 Дождаться статуса «Активен» или «Ready» | ❌ | Промпт: `Дождись, пока статус модели не сменится на «Активен» (или «Ready»). Это может занять несколько минут.` |
| 1.1.5 Скопировать API-эндпоинт модели | ❌ | Промпт: `Скопируй сгенерированный API URL для доступа к модели. Он должен выглядеть как https://api.qubu.ai/.... Сохрани его для следующего шага.` |
| 1.1.6 Протестировать доступность модели через API (curl) | ❌ | Промпт: `Выполни в терминале curl запрос: curl -X POST {API_URL} -H "Content-Type: application/json" -d '{"prompt": "Скажи Привет, мир!", "max_tokens": 50}'. Убедись, что приходит JSON-ответ с полем response.` |

### 1.2 Хостинг для сайтов (Beget.com)

| Задача | Статус | Примечание |
|---|---|---|
| 1.2.1 Зарегистрировать 12 аккаунтов на Beget.com (по одному на сайт) | ❌ | Промпт: `Зарегистрируй новый аккаунт на https://beget.com/ru. Email: ..., Пароль: .... После регистрации выйди из аккаунта.` |
| 1.2.2 Убедиться, что на всех аккаунтах тариф поддерживает PHP >= 7.4 | ❌ | Промпт: `В панели управления сайтом найди раздел «Настройки PHP» и проверь версию. Если ниже 7.4, обнови её.` |
| 1.2.3 Убедиться, что `curl` включен в настройках PHP | ❌ | Промпт: `В панели управления сайтом перейди в «Настройки PHP» и поставь галочку напротив расширения curl, если она не активна.` |
| 1.2.4 Записать логины, пароли и домены в таблицу `accounts.csv` в папке `docs` | ❌ | |

---

## 2. Развертывание PHP-скриптов

### 2.1 Основной скрипт-сборщик (`fetch_and_rewrite.php`)

| Задача | Статус | Примечание |
|---|---|---|
| 2.1.1 Создать файл `scripts/fetch_and_rewrite.php` с защитой по ключу | ❌ | Промпт: `Скопируй код из раздела 2.1.1 файла PROJECT.md в scripts/fetch_and_rewrite.php.` |
| 2.1.2 Настроить переменную `$api_url` на эндпоинт Qwen 2.5 | ❌ | Промпт: `В файле scripts/fetch_and_rewrite.php замени значение переменной $api_url на {API_URL_из_пункта_1.1.5}.` |
| 2.1.3 Настроить `$model` на `qwen2.5:7b-instruct-q5_K_M` | ❌ | Промпт: `Убедись, что в файле scripts/fetch_and_rewrite.php значение переменной $model = 'qwen2.5:7b-instruct-q5_K_M';.` |
| 2.1.4 Настроить `$secret_key` на уникальный ключ для каждого сайта | ❌ | Промпт: `Сгенерируй уникальный случайный ключ длиной 32 символа для этого сайта и вставь его в $secret_key.` |
| 2.1.5 Создать массив из 12 RSS-лент (по одной на тематику) | ❌ | Промпт: `В файле scripts/fetch_and_rewrite.php вставь массив $rss_feeds с 12 RSS-лентами согласно списку в PROJECT.md.` |
| 2.1.6 Проверить обработку ошибок curl и парсинга RSS | ❌ | Промпт: `Убедись, что в коде есть проверки if (!$rss) { error_log(...); exit; } и if (curl_errno($ch)) { ... }.` |
| 2.1.7 Залить скрипт в корень каждого сайта через FTP | ❌ | Промпт: `Используя FTP-клиент (например, FileZilla), загрузи файлы из scripts/ в папку public_html для каждого из 12 сайтов. Данные для FTP возьми из accounts.csv.` |

### 2.2 Генератор статических страниц (`render.php`)

| Задача | Статус | Примечание |
|---|---|---|
| 2.2.1 Создать файл `scripts/render.php` с шаблонизатором | ❌ | Промпт: `Скопируй код из раздела 2.2 файла PROJECT.md в scripts/render.php.` |
| 2.2.2 Создать файл `assets/template.html` с базовой версткой | ❌ | Промпт: `Создай HTML-шаблон с местозаполнителем {{NEWS_CONTENT}} для списка новостей. Добавь заголовок сайта и футер.` |
| 2.2.3 Настроить путь к папке `news/` и проверить права на запись | ❌ | Промпт: `Убедись, что папка news/ существует в public_html каждого сайта и права на неё 755.` |
| 2.2.4 Залить `render.php` и `template.html` на каждый сайт | ❌ | Промпт: `Загрузи файлы render.php и template.html в public_html каждого сайта через FTP.` |

### 2.3 Точка входа для внешнего Cron (`index.php`)

| Задача | Статус | Примечание |
|---|---|---|
| 2.3.1 Создать файл `scripts/index.php` с вызовом `fetch_and_rewrite.php` | ❌ | Промпт: `Создай файл index.php с проверкой ключа из GET-параметра и вызовом fetch_and_rewrite.php.` |
| 2.3.2 Настроить редирект на `index.html` после генерации | ❌ | Промпт: `В конце скрипта добавь header('Location: /index.html');.` |
| 2.3.3 Залить `index.php` в корень каждого сайта | ❌ | Промпт: `Загрузи index.php в public_html каждого сайта через FTP.` |

---

## 3. Автоматизация (Внешний Cron)

| Задача | Статус | Примечание |
|---|---|---|
| 3.1 Зарегистрироваться на cron-job.org (или healthchecks.io) | ❌ | Промпт: `Перейди на https://cron-job.org, создай бесплатный аккаунт.` |
| 3.2 Создать 12 задач (по одной на сайт) | ❌ | Промпт: `В Cronjob.org создай новое задание: URL = https://<домен_сайта>/index.php?key=<секретный_ключ>, Интервал = 1 час. Повтори для каждого из 12 сайтов.` |
| 3.3 Протестировать ручной запуск одной задачи | ❌ | Промпт: `Нажми кнопку «Run now» для первой задачи и убедись, что на сайте обновилась главная страница.` |
| 3.4 Убедиться, что все задачи выполняются без ошибок | ❌ | Промпт: `Проверь логи в Cronjob.org для каждой из 12 задач. Убедись, что статус ответа HTTP 200.` |

---

## 4. Тестирование и отладка

| Задача | Статус | Примечание |
|---|---|---|
| 4.1 Проверить качество рерайта для первых 3 новостей на каждом сайте | ❌ | Промпт: `Открой главную страницу каждого сайта и прочитай первую новость. Убедись, что она связная и уникальная.` |
| 4.2 Проверить, что старые новости не перезаписываются | ❌ | Промпт: `Дождись второго запуска cron-задачи и проверь, что в папке news появился новый .html файл, а старый не изменился.` |
| 4.3 Проверить логи на отсутствие критических ошибок | ❌ | Промпт: `Открой файл error_log.txt на любом сайте и убедись, что там нет записей о фатальных ошибках.` |

---

## 5. Мониторинг и поддержка

| Задача | Статус | Примечание |
|---|---|---|
| 5.1 Настроить еженедельную проверку доступности всех 12 сайтов | ❌ | Промпт: `Создай в Cronjob.org задачу, которая раз в неделю проверяет HTTP 200 со всех 12 доменов и присылает сводку на email.` |
| 5.2 Настроить уведомления при сбоях | ❌ | Промпт: `В Cronjob.org для каждой задачи включи опцию «Уведомлять при ошибках» на email владельца проекта.` |

---
